name: swift

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test_macos:
    runs-on: macos-12
    timeout-minutes: 30
    
    steps:
    - uses: actions/checkout@v2
      
    - uses: Didstopia/SwiftAction@v1.0.3
      with:
        swift-action: 'make test'
